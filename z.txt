Create a Jupyter notebook that demonstrates a simple scoring and ranking system for the Dishify recipe recommendation app.

The notebook should be self-contained and use only simple Python libraries such as pandas, numpy, and optionally matplotlib.

Goal:
Show how Dishify can rank recipe candidates after vector retrieval and hard filtering.

Notebook structure:

1. Introduction
Explain that Dishify first applies hard filters, then computes a weighted score for each recipe.
Mention that LLMs should only be used later for explanations or optional re-ranking of the top results.

2. Create toy recipe data
Create a small in-memory dataset with around 8–10 recipes.
Each recipe should have:
- title
- ingredients as a list of normalized ingredient strings
- cooking_time_minutes
- cuisine
- tags, for example vegetarian, spicy, high_protein, quick
- qdrant_similarity_score as a float between 0 and 1
- quality_score as a float between 0 and 1

Example recipes can include:
- Chicken Rice Bowl
- Tomato Pasta
- Veggie Stir Fry
- Spicy Chicken Tacos
- Creamy Mushroom Pasta
- Egg Fried Rice
- Lentil Curry
- Salmon Salad

3. Define user input
Create example user input:
- available ingredients: chicken, rice, tomato, onion
- excluded ingredients: mushroom
- preferred max cooking time: 30 minutes
- preferences: spicy, high_protein
- preferred cuisines: asian, mexican

4. Hard filtering
Write a function that removes recipes containing excluded ingredients.
Also optionally remove recipes that violate hard constraints.
Print the recipes before and after filtering.

5. Ingredient scoring
Implement:

available_ingredient_coverage =
    matched_user_ingredients / total_user_ingredients

recipe_ingredient_coverage =
    matched_recipe_ingredients / total_recipe_ingredients

ingredient_fit =
    0.70 * available_ingredient_coverage
  + 0.30 * recipe_ingredient_coverage

Show the matched and missing ingredients for each recipe.

6. Semantic similarity
Use the existing qdrant_similarity_score from the toy dataset.
Normalize or clamp it to the range 0–1.

7. Preference fit
Implement a simple preference score:
- Count how many user preferences match recipe tags or cuisine.
- Divide by the total number of preferences.
- If the user has no preferences, return 0.5 as neutral value.

8. Time fit
Implement:

if recipe_time <= preferred_time:
    time_fit = 1.0
else:
    time_fit = preferred_time / recipe_time

If no preferred time is given, return 0.5 as neutral value.

9. Quality fit
Use the recipe quality_score from the toy dataset.
Explain that in the real app this could come from rating, reviews, completeness, image availability, or source reliability.

10. Final scoring
Implement:

final_score =
    0.40 * ingredient_fit
  + 0.25 * semantic_similarity
  + 0.15 * preference_fit
  + 0.10 * time_fit
  + 0.10 * quality_fit

Create a dataframe showing:
- title
- matched ingredients
- missing ingredients
- ingredient_fit
- semantic_similarity
- preference_fit
- time_fit
- quality_fit
- final_score

Sort by final_score descending.

11. Explanation output
For the top 3 recipes, generate a simple rule-based explanation, for example:
"This recipe ranks highly because it uses 3 of your available ingredients, has high semantic similarity, fits your spicy preference, and can be cooked within 30 minutes."

Do not use an actual LLM. Just simulate the explanation with Python strings.

12. Visualization
Add one simple bar chart showing the final_score of each recipe.

13. Conclusion
Briefly explain:
- Hard filters handle strict constraints like allergies or excluded ingredients.
- Weighted scoring handles soft preferences.
- Qdrant provides semantic similarity.
- The LLM can later explain the top results but should not replace deterministic ranking.

Please write clean, readable notebook-style Python code with markdown explanations between sections.
Use functions for each scoring component.
Make sure the notebook can run from top to bottom without external files or APIs.

final_score =
    0.45 * ingredient_fit
  + 0.30 * semantic_similarity
  + 0.15 * preference_fit
  + 0.10 * time_fit