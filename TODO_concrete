Must-do before launch

Auth hardening (Phase 2) — Disable DISABLE_AUTH=false, fix Keycloak public URLs, add token refresh + logout endpoints, password reset flow, rate limiting on login/recommend

Testing & CI (Phase 7) — Write e2e_api_audit.py, wire tests into GitHub Actions CI, contract tests, load testing /recommend

Deployment (Phase 7/10) — Staging environment, production deployment (hosting, TLS, domain), secrets management (.env to a vault)


Should-do before launch

Recommendation quality (Phase 5) — Enable LLM reasoning with OpenRouter, tune ranking weights, validate restriction filters

Documentation (Phase 9) — Update READMEs for frontend/iOS, production runbook, demo script for stakeholders
